# Copilot Instructions

## Project Guidelines
- When asked to convert to WPF for this workspace, keep the existing WinForms app intact and add WPF as a separate app project.
- Locate must be available from the taskbar icon menu (off-screen recovery), not only from the in-app context menu.